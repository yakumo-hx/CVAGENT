# Testing System

Use this folder for reusable verification plans.

For each upgrade, create a private run record under `.local_runs/<run_id>/tests/` with:

- test scope
- commands or UI paths executed
- expected result
- actual result
- pass/fail status
- follow-up notes

Keep personal resume/JD content out of this committed folder.
