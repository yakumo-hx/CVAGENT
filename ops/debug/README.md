# Debug System

Every debug record must follow this sequence:

1. Problem record
2. Code review
3. Fix
4. Resolved status

Each step should capture the action taken and the result observed.

Private records with resume/JD/API content belong under `.local_runs/<run_id>/debug/`.
