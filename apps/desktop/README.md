# Desktop Version

Pure local Tkinter desktop version of Resume Evidence Agent.

Run from repository root:

```powershell
python apps/desktop/desktop_app.py
```

Build Windows exe:

```powershell
.\scripts\build_desktop.ps1
```

The generated executable is written to `release/desktop/CVAGENT-Desktop.exe`.
